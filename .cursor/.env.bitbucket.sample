# Bitbucket MCP – copy to .env.bitbucket and fill in values.
# Create a token: Bitbucket → Personal settings → App passwords, or use an access token.

BITBUCKET_TOKEN=your-bitbucket-token
BITBUCKET_WORKSPACE=your-workspace-slug
